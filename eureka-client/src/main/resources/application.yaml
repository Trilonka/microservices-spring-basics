server:
  port: 0

# application id
spring:
  application:
    name: "eclient"

# service load target
eureka:
  client:
    service-url:
      defaultZone: "http://localhost:8761/eureka"
  instance:
    instance-id: ${spring.application.name}:${random.uuid}